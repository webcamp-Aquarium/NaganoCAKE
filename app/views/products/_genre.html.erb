<div class="col-xs-2">
  <table class="table">
    <thead>
      <tr>
        <th class="active">ジャンル検索</th>
      </tr>
    </thead>
    <tbody>
      <% genres.each do |genre| %>
      <tr>
        <td><%= link_to genre.name,"/products/#{genre.id}/search",class:"genre-search" %></td>
      </tr>
      <% end %>
    </tbody>
  </table>
</div>
