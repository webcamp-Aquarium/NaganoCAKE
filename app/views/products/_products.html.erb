<div class="row">
<% products.each do |product| %>

  <div class="col-xs-3">
    <%= link_to product_path(product) do %>
      <%= attachment_image_tag product, :image, :fill, 200, 150, fallback: "no_image.jpg" %></br>
      <%= product.name %></br>
      ¥<%= product.price %>
    <% end %>
  </div>

<% end %>
</div>
